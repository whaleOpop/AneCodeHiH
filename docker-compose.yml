version: '3.8'

services:
  django:
    build:
      context: .
    container_name: django-app-anecode
    ports:
      - "8000:8000"
    volumes:
      - .:/app
    command: python manage.py runserver 0.0.0.0:8000
